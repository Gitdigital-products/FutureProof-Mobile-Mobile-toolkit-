{
  "name": "@futureproof/mobile-toolkit",
  "version": "0.1.0",
  "description": "Next-gen Solana mobile development toolkit for GitDigital products.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "lint": "eslint src/**/*.ts",
    "test": "jest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@solana/kit": "^2.0.0",
    "@solana/web3-compat": "^1.0.0",
    "lucide-react-native": "^0.400.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsup": "^8.0.0",
    "jest": "^29.7.0",
    "eslint": "^9.0.0",
    "rimraf": "^5.0.0"
  }
}
